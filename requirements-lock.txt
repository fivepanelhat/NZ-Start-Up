# G5/T7* — Supply-chain lock.
# Core runtime now includes cryptography for Fernet backups.
# Install: pip install -r requirements-lock.txt
# Audit:   pip-audit -r requirements-lock.txt

# runtime
cryptography==48.0.1

# dev / CI
pytest==9.0.3
ruff==0.12.2
pip-audit==2.9.0

# optional extras (pin when used)
mcp==1.23.0
pypdf==6.13.3
pywebview==5.4
